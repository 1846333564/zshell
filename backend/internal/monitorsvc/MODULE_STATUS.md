# 监控服务模块状态

## 范围

通过 SSH 命令快照采集 Linux 主机监控信息。

## 重要文件

- `service.go`

## 当前状态

采集 hostname、服务器时间、时区、OS、内核、uptime、CPU 负载百分比、内存百分比、根分区磁盘百分比、按内存/CPU 排序的前 5 个进程、网络字节速率和文件系统分区。采集命令复用共享 SSH 客户端，并且只读取 `/proc`、`date`、`uname`、`/etc/os-release`、`df` 和 `ps` 的轻量信息。

## 已知工作

网络速度基于后端两次采样间隔计算，第一次采样通常显示 0。
