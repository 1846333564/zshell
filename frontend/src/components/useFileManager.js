import{computed,nextTick,onBeforeUnmount,onMounted,reactive,ref,watch}from"vue";import{archiveRemoteItemsUrl,backendDownloadUrl,deleteRemoteItems,downloadRemoteFile,downloadRemoteItems,listRemoteFiles,readRemoteTextFileWithProgress,saveRemoteTextFile,transferRemoteItems,uploadRemoteItems}from"../services/apiClient";import{viewportContextMenuPosition}from"../utils/contextMenuPosition";const ROOT_PATH="/";const HOME_PATH="~";const WORK_MODE_START_PATHS={frontend:"/var",backend:"/opt",ops:ROOT_PATH};const CLIPBOARD_KEY="zshell.remote-file.clipboard.v1";const DIRECTORY_CACHE_LIMIT=1200;const MAX_PRELOAD_CONCURRENCY=4;const TREE_INDEX_BATCH_SIZE=60;const TREE_INDEX_DELAY_MS=16;const PRELOAD_BATCH_SIZE=40;const PRELOAD_BATCH_DELAY_MS=180;const REFRESH_BATCH_SIZE=8;const REFRESH_BATCH_DELAY_MS=80;const MIN_COLUMN_WIDTH=70;const UPLOAD_CLOSE_DELAY_MS=1200;const SORT_PULSE_MS=220;const DEFAULT_FILE_OPEN_ACTION="textEdit";const DEFAULT_EDITOR_WIDTH=980;const DEFAULT_EDITOR_HEIGHT=660;const MIN_EDITOR_TOP=48;const CLIPBOARD_ACTIONS=new Set(["copy","move"]);const directoryCache=new Map;const columns=[{key:"name",label:"\u540D\u79F0",width:280},{key:"type",label:"\u7C7B\u578B",width:82},{key:"size",label:"\u5927\u5C0F",width:110},{key:"modTime",label:"\u4FEE\u6539\u65F6\u95F4",width:175},{key:"mode",label:"\u6743\u9650",width:125},{key:"owner",label:"\u6240\u5C5E\u7528\u6237",width:110}];const fileOpenActions=[{key:"textEdit",label:"\u5728\u7EBF\u7F16\u8F91"}];function useFileManager(props){const entries=ref([]);const currentPath=ref(initialPathForMode(props.workMode));const pathDraft=ref(currentPath.value);const loading=ref(false);const refreshingCached=ref(false);const uploading=ref(false);const dragOver=ref(false);const errorMessage=ref("");const navCollapsed=ref(false);const selectedPaths=ref(new Set);const lastSelectedIndex=ref(-1);const clipboard=ref(readClipboard());const fileInput=ref(null);const folderInput=ref(null);const pathHistoryButton=ref(null);const pathHistoryListRef=ref(null);const pendingUploadPath=ref("");const pathHistoryStats=ref(new Map);const pathHistoryOpen=ref(false);const columnWidths=reactive(loadColumnWidths());const sortState=reactive({key:"name",direction:"asc"});const sortPulseKey=ref("");const uploadProgress=reactive({visible:false,expanded:false,status:"idle",targetPath:"",files:[],directoryCount:0,totalBytes:0,loadedBytes:0,speed:0,startedAt:0,message:""});const editors=ref([]);const activeEditorId=ref("");const pathMeta=ref(initialPathMeta(currentPath.value));let resizeState=null;let editorDragState=null;let uploadCloseTimer=null;let editorZIndex=900;let refreshSerial=0;let preloadSerial=0;let preloadController=null;let treeIndexQueue=[];let treeIndexTimer=null;let sortPulseTimer=null;const contextMenu=reactive({visible:false,x:0,y:0,entry:null,targetPath:"",targetKind:"blank"});const pathHistoryPanel=reactive({x:0,y:0,width:320,height:260});watch(()=>[props.connectionId,props.workMode],async([value,workMode])=>{clearSelection();cancelDirectoryPreload();const startPath=initialPathForMode(workMode);resetPathState(startPath);if(!value){entries.value=[];currentPath.value=startPath;errorMessage.value="";return}await refresh(startPath)},{immediate:true});watch(currentPath,value=>{pathDraft.value=value});const orderedEntries=computed(()=>[...entries.value].sort(compareEntriesForSort));const selectedEntries=computed(()=>orderedEntries.value.filter(entry=>selectedPaths.value.has(entry.path)));const hasSelection=computed(()=>selectedEntries.value.length>0);const canPaste=computed(()=>Boolean(props.connectionId&&clipboard.value?.items?.length));const contextEntry=computed(()=>contextMenu.entry);const contextCanOpenDirectory=computed(()=>Boolean(contextMenu.entry?.isDir||contextMenu.targetKind==="directory"));const contextPath=computed(()=>contextMenu.entry?.path||contextMenu.targetPath||defaultDirectoryPath());const contextTargetDir=computed(()=>contextMenu.entry?.isDir?contextMenu.entry.path:contextMenu.targetPath||defaultDirectoryPath());const contextDeleteItem=computed(()=>{if(hasSelection.value||contextMenu.entry){return null}const targetPath=normalizePath(contextMenu.targetPath);if(contextMenu.targetKind!=="directory"||!isDeletableRemotePath(targetPath)){return null}return{path:targetPath,isDir:true}});const canDeleteFromMenu=computed(()=>hasSelection.value||Boolean(contextDeleteItem.value));const gridTemplateColumns=computed(()=>columns.map(column=>`${columnWidths[column.key]}px`).join(" "));const activeEditor=computed(()=>editors.value.find(item=>item.id===activeEditorId.value)||null);const preloadConcurrency=computed(()=>Math.min(MAX_PRELOAD_CONCURRENCY,Math.max(1,Math.round(Number(props.hardware?.cpuThreads)||1))));const statusText=computed(()=>{if(!props.connectionId){return"\u672A\u8FDE\u63A5"}if(uploading.value){return`\u4E0A\u4F20 ${uploadPercent.value}% \xB7 ${formatUploadSpeed(uploadProgress.speed)}`}if(refreshingCached.value){return`\u7F13\u5B58 ${orderedEntries.value.length} \u9879 \xB7 \u6B63\u5728\u66F4\u65B0`}if(loading.value){return"\u8BFB\u53D6\u4E2D..."}if(clipboard.value?.items?.length){return`${clipboard.value.action==="move"?"\u526A\u5207":"\u590D\u5236"}\u4E86 ${clipboard.value.items.length} \u9879`}return`${orderedEntries.value.length} \u9879`});const breadcrumbs=computed(()=>buildBreadcrumbs(currentPath.value));const pathHistoryStyle=computed(()=>({left:`${pathHistoryPanel.x}px`,top:`${pathHistoryPanel.y}px`,width:`${pathHistoryPanel.width}px`,height:`${pathHistoryPanel.height}px`}));const uploadPercent=computed(()=>{if(uploadProgress.status==="done"){return 100}if(uploadProgress.totalBytes<=0){return 0}return Math.min(100,Math.max(0,Math.round(uploadProgress.loadedBytes/uploadProgress.totalBytes*100)))});const uploadTitle=computed(()=>{if(uploadProgress.status==="done"){return"\u4E0A\u4F20\u5B8C\u6210"}if(uploadProgress.status==="error"){return"\u4E0A\u4F20\u5931\u8D25"}return`\u4E0A\u4F20 ${uploadProgress.files.length+uploadProgress.directoryCount} \u9879`});const uploadDetail=computed(()=>{if(uploadProgress.status==="done"){return"\u5DF2\u5B8C\u6210\uFF0C\u7A0D\u540E\u81EA\u52A8\u6298\u53E0"}if(uploadProgress.status==="error"){return"\u8BF7\u68C0\u67E5\u8FDE\u63A5\u6216\u8FDC\u7A0B\u76EE\u5F55\u6743\u9650"}return`\u603B\u8FDB\u5EA6 ${uploadPercent.value}%`});const pathHistory=computed(()=>Array.from(pathHistoryStats.value.entries()).map(([itemPath,stats])=>({path:itemPath,count:Number(stats?.count)||0,lastVisited:Number(stats?.lastVisited)||0})).sort((left,right)=>{if(left.count!==right.count){return left.count-right.count}if(left.lastVisited!==right.lastVisited){return left.lastVisited-right.lastVisited}return comparePaths(left.path,right.path)}).map(item=>item.path));function compareEntriesForSort(left,right){const direction=sortState.direction==="desc"?-1:1;if(sortState.key!=="type"&&left.isDir!==right.isDir){return left.isDir?-1:1}const primary=compareEntryValue(left,right,sortState.key);if(primary!==0){return primary*direction}return compareEntryValue(left,right,"name")}function compareEntryValue(left,right,key){switch(key){case"type":if(left.isDir!==right.isDir){return left.isDir?-1:1}return compareText(left.name,right.name);case"size":return compareNumber(left.isDir?-1:Number(left.size)||0,right.isDir?-1:Number(right.size)||0);case"modTime":return compareNumber(Date.parse(left.modTime)||0,Date.parse(right.modTime)||0);case"mode":return compareText(left.mode,right.mode);case"owner":return compareText(left.owner,right.owner);case"name":default:return compareText(left.name,right.name)}}function compareText(left,right){return String(left||"").localeCompare(String(right||""),void 0,{sensitivity:"base",numeric:true})}function compareNumber(left,right){if(left===right){return 0}return left<right?-1:1}function changeSort(key){if(sortState.key===key){sortState.direction=sortState.direction==="asc"?"desc":"asc"}else{sortState.key=key;sortState.direction="asc"}pulseSortColumn(key)}function sortArrow(key){if(sortState.key!==key){return""}return sortState.direction==="asc"?"\u2191":"\u2193"}function pulseSortColumn(key){clearSortPulseTimer();sortPulseKey.value=key;sortPulseTimer=window.setTimeout(()=>{sortPulseKey.value="";sortPulseTimer=null},SORT_PULSE_MS)}function clearSortPulseTimer(){if(sortPulseTimer){window.clearTimeout(sortPulseTimer);sortPulseTimer=null}}const treeNodes=computed(()=>{const paths=Array.from(pathMeta.value.keys()).filter(Boolean).sort(comparePaths);const parentSet=new Set(paths.map(itemPath=>parentPath(itemPath)).filter(Boolean));return paths.filter(itemPath=>isPathVisible(itemPath)).map(itemPath=>{const meta=pathMeta.value.get(itemPath)||{};return{path:itemPath,depth:pathDepth(itemPath),opened:Boolean(meta.opened),collapsed:Boolean(meta.collapsed),hasChildren:parentSet.has(itemPath)}})});onMounted(()=>{applyColumnWidths();window.addEventListener("storage",onStorage);window.addEventListener("keydown",onKeydown);window.addEventListener("pointerdown",onGlobalPointerDown,true)});onBeforeUnmount(()=>{window.removeEventListener("storage",onStorage);window.removeEventListener("keydown",onKeydown);window.removeEventListener("pointerdown",onGlobalPointerDown,true);clearUploadCloseTimer();clearTreeIndexQueue();clearSortPulseTimer();cancelDirectoryPreload();stopColumnResize();stopEditorDrag()});async function refresh(targetPath=currentPath.value||initialPathForMode(props.workMode),options={}){if(!props.connectionId){return}if(options.cancelPreload!==false){cancelDirectoryPreload()}const requestedPath=targetPath||initialPathForMode(props.workMode);const serial=refreshSerial+=1;const cached=options.useCache===false?null:getCachedDirectory(props.connectionId,requestedPath);if(cached){applyDirectoryListing(cached.path,cached.entries,cached.requestedPath||requestedPath);loading.value=false;refreshingCached.value=true}else{loading.value=true;refreshingCached.value=false}errorMessage.value="";try{const result=await loadDirectoryFromRemote(requestedPath,{rememberTree:true});if(serial!==refreshSerial){return}applyDirectoryListing(result.resolvedPath,result.entries,requestedPath);scheduleDirectoryPreload()}catch(error){if(serial===refreshSerial){errorMessage.value=error instanceof Error?error.message:"\u8BFB\u53D6\u76EE\u5F55\u5931\u8D25"}}finally{if(serial===refreshSerial){loading.value=false;refreshingCached.value=false}}}async function loadDirectoryFromRemote(requestedPath,options={}){const result=await listRemoteFiles(props.connectionId,requestedPath,{signal:options.signal});const resolvedPath=result.path||requestedPath;const nextEntries=Array.isArray(result.entries)?result.entries:[];setCachedDirectory(props.connectionId,requestedPath,resolvedPath,nextEntries,{rememberTree:options.rememberTree});if(resolvedPath!==requestedPath){setCachedDirectory(props.connectionId,resolvedPath,resolvedPath,nextEntries,{rememberTree:false})}return{requestedPath,resolvedPath,entries:nextEntries}}async function refreshOpenDirectories(focusPath=currentPath.value||initialPathForMode(props.workMode)){if(!props.connectionId){return}cancelDirectoryPreload();const serial=refreshSerial+=1;const requestedFocus=focusPath||currentPath.value||initialPathForMode(props.workMode);const normalizedFocus=normalizePath(requestedFocus);const targets=openedDirectoryPaths(requestedFocus);let focusListing=null;let focusError=null;const failures=[];loading.value=true;refreshingCached.value=false;errorMessage.value="";try{for(let index=0;index<targets.length;index+=REFRESH_BATCH_SIZE){const batch=targets.slice(index,index+REFRESH_BATCH_SIZE);const results=await Promise.all(batch.map(async targetPath=>{try{return await loadDirectoryFromRemote(targetPath,{rememberTree:true})}catch(error){return{requestedPath:targetPath,error}}}));if(serial!==refreshSerial){return}for(const result of results){if(result.error){failures.push(result);if(normalizePath(result.requestedPath)===normalizedFocus){focusError=result.error}continue}if(directoryListingMatchesFocus(result,normalizedFocus)){focusListing=result}}if(index+REFRESH_BATCH_SIZE<targets.length){await delay(REFRESH_BATCH_DELAY_MS)}}if(focusError){throw focusError}if(focusListing){applyDirectoryListing(focusListing.resolvedPath,focusListing.entries,focusListing.requestedPath)}else{const cached=getCachedDirectory(props.connectionId,requestedFocus);if(cached){applyDirectoryListing(cached.path,cached.entries,cached.requestedPath||requestedFocus)}}if(failures.length>0){errorMessage.value=`${failures.length} \u4E2A\u5DF2\u6253\u5F00\u76EE\u5F55\u5237\u65B0\u5931\u8D25`}scheduleDirectoryPreload()}catch(error){if(serial===refreshSerial){errorMessage.value=error instanceof Error?error.message:"\u5237\u65B0\u76EE\u5F55\u5931\u8D25"}}finally{if(serial===refreshSerial){loading.value=false;refreshingCached.value=false}}}function openedDirectoryPaths(focusPath){const seen=new Set;const result=[];const add=itemPath=>{const normalized=normalizePath(itemPath);if(!normalized||seen.has(normalized)){return}seen.add(normalized);result.push(normalized)};add(focusPath);add(currentPath.value);for(const[itemPath,meta]of pathMeta.value.entries()){if(meta?.opened){add(itemPath)}}return result}function directoryListingMatchesFocus(listing,normalizedFocus){if(!normalizedFocus||!listing){return false}return normalizePath(listing.requestedPath)===normalizedFocus||normalizePath(listing.resolvedPath)===normalizedFocus}function applyDirectoryListing(resolvedPath,nextEntries,requestedPath=resolvedPath){currentPath.value=resolvedPath;entries.value=cloneEntries(nextEntries);rememberPathHistory(resolvedPath);rememberPath(ROOT_PATH);rememberPath(HOME_PATH);if(requestedPath===HOME_PATH||requestedPath.startsWith("~/")){rememberPath(HOME_PATH,{opened:true})}rememberDirectoryTree(resolvedPath,entries.value);keepExistingSelection()}function resetPathState(startPath=initialPathForMode(props.workMode)){clearTreeIndexQueue();pathMeta.value=initialPathMeta(startPath);pathHistoryStats.value=new Map;pathHistoryOpen.value=false}function initialPathForMode(workMode){return WORK_MODE_START_PATHS[normalizeWorkMode(workMode)]||ROOT_PATH}function defaultDirectoryPath(){return currentPath.value||initialPathForMode(props.workMode)}function normalizeWorkMode(workMode){return["frontend","backend","ops"].includes(workMode)?workMode:"ops"}function initialPathMeta(startPath){const meta=new Map([[ROOT_PATH,{opened:false,collapsed:false}],[HOME_PATH,{opened:false,collapsed:false}]]);const normalized=normalizePath(startPath);if(normalized&&!meta.has(normalized)){meta.set(normalized,{opened:false,collapsed:false})}return meta}function getCachedDirectory(connectionId,remotePath){const cached=directoryCache.get(directoryCacheKey(connectionId,remotePath));if(!cached){return null}return{...cached,entries:cloneEntries(cached.entries)}}function setCachedDirectory(connectionId,requestedPath,resolvedPath,nextEntries,options={}){const key=directoryCacheKey(connectionId,requestedPath);if(!key){return}const resolved=normalizePath(resolvedPath||requestedPath);const entriesForCache=cloneEntries(nextEntries);if(directoryCache.has(key)){directoryCache.delete(key)}directoryCache.set(key,{requestedPath,path:resolved,entries:entriesForCache,cachedAt:Date.now()});if(options.rememberTree!==false){rememberDirectoryTree(resolved,entriesForCache)}while(directoryCache.size>DIRECTORY_CACHE_LIMIT){const oldestKey=directoryCache.keys().next().value;directoryCache.delete(oldestKey)}}function directoryCacheKey(connectionId,remotePath){const normalized=normalizePath(remotePath||initialPathForMode(props.workMode));if(!connectionId||!normalized){return""}return`${connectionId}\0${normalized}`}function cloneEntries(value){return Array.isArray(value)?value.map(entry=>({...entry})):[]}function rememberDirectoryTree(resolvedPath,nextEntries){const next=new Map(pathMeta.value);rememberParentChainInMap(next,resolvedPath);mergePathMeta(next,resolvedPath,{opened:true,collapsed:false});pathMeta.value=next;const childDirs=nextEntries.filter(entry=>entry.isDir).map(entry=>normalizePath(entry.path)).filter(Boolean);enqueueTreeIndex(childDirs)}function scheduleDirectoryPreload(){cancelDirectoryPreload();const targets=directoryPreloadTargets();if(targets.length===0){return}const serial=preloadSerial+=1;const controller=new AbortController;preloadController=controller;const queue=[...targets];const workerCount=Math.min(preloadConcurrency.value,queue.length);const workers=Array.from({length:workerCount},async()=>{let completedInBatch=0;while(queue.length>0&&preloadSerial===serial&&!controller.signal.aborted){const targetPath=queue.shift();if(!targetPath||getCachedDirectory(props.connectionId,targetPath)){continue}try{await loadDirectoryFromRemote(targetPath,{signal:controller.signal,rememberTree:false});if(preloadSerial!==serial||controller.signal.aborted){return}completedInBatch+=1;if(completedInBatch>=PRELOAD_BATCH_SIZE&&queue.length>0){completedInBatch=0;await delay(PRELOAD_BATCH_DELAY_MS)}}catch(error){if(error?.name==="AbortError"||controller.signal.aborted){return}}}});Promise.allSettled(workers).finally(()=>{if(preloadSerial===serial){preloadController=null}})}function cancelDirectoryPreload(){preloadSerial+=1;if(preloadController){preloadController.abort();preloadController=null}}function directoryPreloadTargets(){const targets=[];const seen=new Set;for(const itemPath of Array.from(pathMeta.value.keys()).sort(comparePaths)){const meta=pathMeta.value.get(itemPath)||{};if(!isPathVisible(itemPath)||!meta.opened&&itemPath!==currentPath.value){continue}const cached=getCachedDirectory(props.connectionId,itemPath);if(!cached){continue}for(const entry of cached.entries){if(!entry.isDir){continue}const normalized=normalizePath(entry.path);if(!normalized||seen.has(normalized)||getCachedDirectory(props.connectionId,normalized)){continue}seen.add(normalized);targets.push(normalized)}}return targets}function enqueueTreeIndex(paths){const jobs=paths.map(itemPath=>normalizePath(itemPath)).filter(itemPath=>itemPath&&!pathMeta.value.has(itemPath));if(jobs.length===0){return}treeIndexQueue.push(...jobs);scheduleTreeIndexWork()}function scheduleTreeIndexWork(delayMs=TREE_INDEX_DELAY_MS){if(treeIndexTimer||treeIndexQueue.length===0){return}treeIndexTimer=window.setTimeout(processTreeIndexBatch,delayMs)}function processTreeIndexBatch(){treeIndexTimer=null;if(treeIndexQueue.length===0){return}const batch=treeIndexQueue.splice(0,TREE_INDEX_BATCH_SIZE);const next=new Map(pathMeta.value);for(const itemPath of batch){rememberParentChainInMap(next,itemPath);mergePathMeta(next,itemPath,{opened:false})}pathMeta.value=next;if(treeIndexQueue.length>0){scheduleTreeIndexWork()}}function clearTreeIndexQueue(){treeIndexQueue=[];if(treeIndexTimer){window.clearTimeout(treeIndexTimer);treeIndexTimer=null}}function rememberPath(itemPath,patch={}){const normalized=normalizePath(itemPath);if(!normalized){return}const next=new Map(pathMeta.value);mergePathMeta(next,normalized,patch);pathMeta.value=next}function rememberParentChain(itemPath){const normalized=normalizePath(itemPath);if(!normalized){return}const chain=[];let cursor=normalized;while(cursor){chain.unshift(cursor);cursor=parentPath(cursor)}const next=new Map(pathMeta.value);for(const segment of chain){mergePathMeta(next,segment)}pathMeta.value=next}function rememberParentChainInMap(target,itemPath){const normalized=normalizePath(itemPath);if(!normalized){return}let cursor=normalized;const chain=[];while(cursor){chain.unshift(cursor);cursor=parentPath(cursor)}for(const segment of chain){mergePathMeta(target,segment)}}function mergePathMeta(target,itemPath,patch={}){const normalized=normalizePath(itemPath);if(!normalized){return}const existing=target.get(normalized)||{opened:false,collapsed:false};target.set(normalized,{...existing,...patch})}function rememberPathHistory(itemPath){const normalized=normalizePath(itemPath);if(!normalized){return}const next=new Map(pathHistoryStats.value);const existing=next.get(normalized)||{count:0,lastVisited:0};next.set(normalized,{count:Number(existing.count)+1,lastVisited:Date.now()});trimPathHistoryStats(next);pathHistoryStats.value=next}function togglePathHistory(){if(!props.connectionId||pathHistory.value.length===0){pathHistoryOpen.value=false;return}if(pathHistoryOpen.value){pathHistoryOpen.value=false;return}positionPathHistoryPanel();pathHistoryOpen.value=true;scrollPathHistoryToBottom()}function positionPathHistoryPanel(){const button=pathHistoryButton.value;if(!button){return}const rect=button.getBoundingClientRect();const width=Math.min(560,Math.max(340,window.innerWidth-24));const availableBelow=Math.max(120,window.innerHeight-rect.bottom-12);const availableAbove=Math.max(120,rect.top-12);const height=Math.min(420,Math.max(180,Math.min(Math.max(availableBelow,availableAbove),pathHistory.value.length*38+42)));pathHistoryPanel.x=Math.min(Math.max(8,rect.left),Math.max(8,window.innerWidth-width-8));pathHistoryPanel.y=availableBelow>=height?rect.bottom+8:Math.max(8,rect.top-height-8);pathHistoryPanel.width=width;pathHistoryPanel.height=height}function trimPathHistoryStats(target){const maxItems=80;if(target.size<=maxItems){return}const ranked=Array.from(target.entries()).sort((left,right)=>{const leftStats=left[1]||{};const rightStats=right[1]||{};const countDelta=(Number(rightStats.count)||0)-(Number(leftStats.count)||0);if(countDelta!==0){return countDelta}return(Number(rightStats.lastVisited)||0)-(Number(leftStats.lastVisited)||0)});for(const[itemPath]of ranked.slice(maxItems)){target.delete(itemPath)}}function scrollPathHistoryToBottom(){nextTick(()=>{const list=pathHistoryListRef.value;if(list){list.scrollTop=list.scrollHeight}})}function openHistoryPath(itemPath){pathHistoryOpen.value=false;clearSelection();refresh(itemPath)}function togglePathNode(itemPath){const next=new Map(pathMeta.value);const existing=next.get(itemPath)||{opened:false,collapsed:false};next.set(itemPath,{...existing,collapsed:!existing.collapsed});pathMeta.value=next}function openDir(itemPath){if(loading.value){return}pathHistoryOpen.value=false;clearSelection();refresh(itemPath)}function openEntry(entry){if(entry.isDir){openDir(entry.path);return}runFileOpenAction(DEFAULT_FILE_OPEN_ACTION,entry)}function onPathInput(event){pathDraft.value=event.target.value||""}function commitPathDraft(event){const value=pathDraft.value.trim();if(!value){pathDraft.value=currentPath.value;return}pathHistoryOpen.value=false;clearSelection();refresh(value)}async function completePathDraft(){if(!props.connectionId){return}const prefix=normalizePath(pathDraft.value);if(!prefix){return}await ensureCompletionCaches(prefix);const matches=pathCompletionMatches(prefix);if(matches.length===1){pathDraft.value=matches[0]}}async function ensureCompletionCaches(prefix){const prefixes=[prefix];const relativePrefix=currentRelativeCompletionPrefix(prefix);if(relativePrefix&&relativePrefix!==prefix){prefixes.push(relativePrefix)}for(const item of prefixes){const parent=completionParentPath(item);if(!parent||getCachedDirectory(props.connectionId,parent)){continue}try{const result=await listRemoteFiles(props.connectionId,parent);const resolvedPath=result.path||parent;const nextEntries=Array.isArray(result.entries)?result.entries:[];setCachedDirectory(props.connectionId,parent,resolvedPath,nextEntries,{rememberTree:false});if(resolvedPath!==parent){setCachedDirectory(props.connectionId,resolvedPath,resolvedPath,nextEntries,{rememberTree:false})}}catch{}}}function pathCompletionMatches(prefix){const candidates=pathCompletionCandidates();let matches=completionMatchesForPrefix(prefix,candidates);if(matches.length===0){const relativePrefix=currentRelativeCompletionPrefix(prefix);if(relativePrefix&&relativePrefix!==prefix){matches=completionMatchesForPrefix(relativePrefix,candidates)}}return matches}function pathCompletionCandidates(){const candidates=new Set([ROOT_PATH,HOME_PATH,...Object.values(WORK_MODE_START_PATHS),...pathHistory.value,...Array.from(pathMeta.value.keys())]);for(const entry of entries.value){if(entry.isDir){candidates.add(entry.path)}}for(const cached of cachedDirectoriesForConnection(props.connectionId)){candidates.add(cached.path);for(const entry of cached.entries){if(entry.isDir){candidates.add(entry.path)}}}return candidates}function completionMatchesForPrefix(prefix,candidates){const matches=new Set;for(const candidate of candidates){const normalized=normalizePath(candidate);const match=completionTarget(prefix,normalized);if(match){matches.add(match)}}return Array.from(matches).sort(comparePaths)}function currentRelativeCompletionPrefix(prefix){const current=normalizePath(currentPath.value);if(!current||current===ROOT_PATH||current===HOME_PATH){return""}const raw=String(prefix||"").trim();if(!raw){return""}const clean=raw.startsWith(ROOT_PATH)?raw.slice(1):raw;if(!clean||clean.includes("/")){return""}return normalizePath(`${current}/${clean}`)}function completionParentPath(prefix){const normalized=normalizePath(prefix);if(!normalized){return""}if(normalized===ROOT_PATH||normalized===HOME_PATH){return""}if(normalized.endsWith("/")&&normalized!==ROOT_PATH){return normalizePath(normalized.slice(0,-1))||ROOT_PATH}return parentPath(normalized)||ROOT_PATH}function completionTarget(prefix,candidate){if(!candidate||candidate===prefix||!candidate.startsWith(prefix)){return""}const remaining=candidate.slice(prefix.length);const slashIndex=remaining.indexOf("/");if(slashIndex===-1){return candidate}return candidate.slice(0,prefix.length+slashIndex)}function cachedDirectoriesForConnection(connectionId){if(!connectionId){return[]}const prefix=`${connectionId}\0`;const result=[];for(const[key,cached]of directoryCache.entries()){if(key.startsWith(prefix)){result.push({...cached,entries:cloneEntries(cached.entries)})}}return result}function syncDirectoryCacheAfterDelete(items){const deleted=items.map(item=>({path:normalizePath(item.path),isDir:Boolean(item.isDir)})).filter(item=>item.path);if(deleted.length===0){return}const prefix=`${props.connectionId}\0`;for(const[key,cached]of Array.from(directoryCache.entries())){if(!key.startsWith(prefix)){continue}const cachedPath=normalizePath(cached.path||cached.requestedPath);if(deleted.some(item=>item.isDir&&isSameOrChildPath(cachedPath,item.path))){directoryCache.delete(key);continue}const nextEntries=cloneEntries(cached.entries).filter(entry=>!deleted.some(item=>entryMatchesDeletedItem(entry,item)));if(nextEntries.length!==cached.entries.length){directoryCache.set(key,{...cached,entries:nextEntries,cachedAt:Date.now()})}}entries.value=entries.value.filter(entry=>!deleted.some(item=>entryMatchesDeletedItem(entry,item)));removeDeletedPathMeta(deleted)}function invalidateDirectoryCache(remotePath){const normalized=normalizePath(remotePath);if(!normalized||!props.connectionId){return}const keys=[directoryCacheKey(props.connectionId,normalized)];for(const key of keys){if(key){directoryCache.delete(key)}}}function entryMatchesDeletedItem(entry,item){const entryPath=normalizePath(entry.path);if(!entryPath||!item.path){return false}return item.isDir?isSameOrChildPath(entryPath,item.path):entryPath===item.path}function removeDeletedPathMeta(deletedItems){const next=new Map(pathMeta.value);for(const key of Array.from(next.keys())){if(deletedItems.some(item=>item.isDir?isSameOrChildPath(key,item.path):key===item.path)){next.delete(key)}}pathMeta.value=next}function pathAfterDeletingItems(items){const current=normalizePath(currentPath.value)||initialPathForMode(props.workMode);for(const item of items){const deletedPath=normalizePath(item.path);if(item.isDir&&isSameOrChildPath(current,deletedPath)){return nearestExistingParentAfterDelete(deletedPath,items)}}return current}function nearestExistingParentAfterDelete(deletedPath,deletedItems){let candidate=parentPath(deletedPath)||ROOT_PATH;while(candidate&&deletedItems.some(item=>item.isDir&&isSameOrChildPath(candidate,normalizePath(item.path)))){candidate=parentPath(candidate)}return candidate||ROOT_PATH}function isDeletableRemotePath(itemPath){const normalized=normalizePath(itemPath);return Boolean(normalized&&normalized!==ROOT_PATH&&normalized!==HOME_PATH)}function chooseFiles(targetPath=contextTargetDir.value){pendingUploadPath.value=targetPath||defaultDirectoryPath();fileInput.value?.click()}function chooseFolder(targetPath=contextTargetDir.value){pendingUploadPath.value=targetPath||defaultDirectoryPath();folderInput.value?.click()}function chooseFilesFromMenu(){const target=contextTargetDir.value;hideContextMenu();chooseFiles(target)}function chooseFolderFromMenu(){const target=contextTargetDir.value;hideContextMenu();chooseFolder(target)}async function onFilePickerUpload(event){const files=Array.from(event.target.files||[]);await uploadPickedFiles(files,[],pendingUploadPath.value||defaultDirectoryPath());event.target.value=""}async function onFolderPickerUpload(event){const files=Array.from(event.target.files||[]);const directories=deriveDirectoriesFromFiles(files);await uploadPickedFiles(files,directories,pendingUploadPath.value||defaultDirectoryPath());event.target.value=""}async function uploadPickedFiles(files,directories=[],targetPath=defaultDirectoryPath()){const items=files.map(file=>({file,relativePath:file.webkitRelativePath||file.name}));await uploadItems(items,directories,targetPath)}async function uploadItems(items,directories=[],targetPath=defaultDirectoryPath()){if(!props.connectionId||items.length===0&&directories.length===0){return}uploading.value=true;errorMessage.value="";startUploadProgress(items,directories,targetPath||defaultDirectoryPath());let succeeded=false;try{await uploadRemoteItems(props.connectionId,targetPath||defaultDirectoryPath(),items,directories,onUploadProgress);succeeded=true;markUploadComplete();invalidateDirectoryCache(targetPath||defaultDirectoryPath());await refresh(currentPath.value||initialPathForMode(props.workMode),{useCache:false})}catch(error){const message=error instanceof Error?error.message:"\u4E0A\u4F20\u5931\u8D25";errorMessage.value=message;markUploadError(message)}finally{uploading.value=false;if(succeeded){scheduleUploadPanelClose()}}}function onDragOver(event){if(!props.connectionId){return}event.dataTransfer.dropEffect="copy";dragOver.value=true}function onDragLeave(event){if(!event.currentTarget.contains(event.relatedTarget)){dragOver.value=false}}async function onDropUpload(event){dragOver.value=false;if(!props.connectionId){return}const{files,directories}=await collectDroppedItems(event.dataTransfer);await uploadItems(files,directories,defaultDirectoryPath())}async function download(itemPath,name){if(!props.connectionId){return}errorMessage.value="";try{await downloadRemoteFile(props.connectionId,itemPath,name)}catch(error){errorMessage.value=error instanceof Error?error.message:"\u4E0B\u8F7D\u5931\u8D25"}}async function downloadSelection(){if(!hasSelection.value){return}const selected=selectedEntries.value;const paths=selected.map(entry=>entry.path);errorMessage.value="";try{if(selected.length===1&&!selected[0].isDir){await downloadRemoteFile(props.connectionId,selected[0].path,selected[0].name);return}await downloadRemoteItems(props.connectionId,paths,downloadArchiveName(selected))}catch(error){errorMessage.value=error instanceof Error?error.message:"\u4E0B\u8F7D\u5931\u8D25"}}async function downloadSelectionFromMenu(){hideContextMenu();await downloadSelection()}async function downloadContextEntry(){const entry=contextMenu.entry;hideContextMenu();if(!entry||entry.isDir){return}await download(entry.path,entry.name)}function openContextDirectory(){const targetPath=contextMenu.entry?.isDir?contextMenu.entry.path:contextMenu.targetPath;hideContextMenu();if(targetPath){openDir(targetPath)}}async function runContextFileOpenAction(actionKey){const entry=contextMenu.entry;hideContextMenu();if(!entry||entry.isDir){return}await runFileOpenAction(actionKey,entry)}async function runFileOpenAction(actionKey,entry){if(actionKey==="textEdit"){await openTextEditor(entry)}}async function openTextEditor(entry){if(!props.connectionId||!entry||entry.isDir){return}const existing=editors.value.find(item=>item.path===entry.path);if(existing){if(existing.windowState==="minimized"){existing.windowState="normal"}activateEditor(existing.id);return}const editorWindow=createEditorWindow(entry);try{updateEditorOpenProgress(editorWindow,{stage:"preparing",totalBytes:Number(entry.size)||0,message:"\u51C6\u5907\u6253\u5F00\u8FDC\u7A0B\u6587\u4EF6"});const result=await readRemoteTextFileWithProgress(props.connectionId,entry.path,progress=>{updateEditorOpenProgress(editorWindow,progress)});const file=result.file||{};const content=String(file.content??"");editorWindow.path=String(file.path||entry.path);editorWindow.name=String(file.name||entry.name||displayPath(editorWindow.path));editorWindow.content=content;editorWindow.originalContent=content;editorWindow.size=Number(file.size)||content.length;editorWindow.modTime=String(file.modTime||entry.modTime||"");editorWindow.message="\u5DF2\u6253\u5F00";updateEditorOpenProgress(editorWindow,{stage:"done",loadedBytes:editorWindow.size,totalBytes:editorWindow.size,message:"\u8FDC\u7A0B\u6587\u4EF6\u4E0B\u8F7D\u5B8C\u6210"})}catch(error){editorWindow.error=error instanceof Error?error.message:"\u6253\u5F00\u6587\u4EF6\u5931\u8D25";editorWindow.message="\u6253\u5F00\u5931\u8D25";updateEditorOpenProgress(editorWindow,{stage:"error",loadedBytes:Number(editorWindow.openProgress?.loadedBytes)||0,totalBytes:Number(editorWindow.openProgress?.totalBytes)||Number(entry.size)||0,message:editorWindow.error})}finally{editorWindow.loading=false}}function createEditorWindow(entry){const bounds=defaultEditorBounds();const editorWindow={id:crypto.randomUUID(),windowState:"normal",loading:true,saving:false,path:entry.path,name:entry.name,content:"",originalContent:"",size:Number(entry.size)||0,modTime:entry.modTime||"",error:"",message:"",editorRuntimeState:"loading",editorRuntimeMessage:"\u52A0\u8F7D Monaco...",editorRuntimeProgress:0,editorRuntimeStep:0,editorRuntimeTotalSteps:0,openProgress:{stage:"preparing",loadedBytes:0,totalBytes:Number(entry.size)||0,startedAt:Date.now(),message:"\u51C6\u5907\u6253\u5F00\u8FDC\u7A0B\u6587\u4EF6"},x:bounds.x,y:bounds.y,width:bounds.width,height:bounds.height,zIndex:nextEditorZIndex(),closePrompt:{visible:false,afterClose:null}};editors.value.push(editorWindow);activeEditorId.value=editorWindow.id;return editorWindow}function defaultEditorBounds(){const viewportWidth=window.innerWidth||1200;const viewportHeight=window.innerHeight||780;const width=Math.min(DEFAULT_EDITOR_WIDTH,Math.max(520,viewportWidth-64));const height=Math.min(DEFAULT_EDITOR_HEIGHT,Math.max(360,viewportHeight-126));const offset=editors.value.length%7*26;const centeredX=Math.max(16,Math.round((viewportWidth-width)/2));return{x:Math.min(Math.max(16,centeredX+offset),Math.max(16,viewportWidth-120)),y:Math.min(MIN_EDITOR_TOP+offset,Math.max(MIN_EDITOR_TOP,viewportHeight-120)),width,height}}function nextEditorZIndex(){editorZIndex+=1;return editorZIndex}function activateEditor(id){const editorWindow=editors.value.find(item=>item.id===id);if(!editorWindow){return}activeEditorId.value=id;editorWindow.zIndex=nextEditorZIndex()}function editorDirty(editorWindow){return Boolean(editorWindow&&editorWindow.content!==editorWindow.originalContent)}function editorTitle(editorWindow){return editorWindow?.name||displayPath(editorWindow?.path)||"\u8FDC\u7A0B\u6587\u4EF6"}function editorStatus(editorWindow){if(editorWindow.loading){return editorOpenStatus(editorWindow)}if(editorWindow.error&&editorWindow.openProgress?.stage==="error"){return"\u6253\u5F00\u5931\u8D25"}if(editorWindow.saving){return"\u4FDD\u5B58\u4E2D..."}if(editorWindow.editorRuntimeState==="loading"){return editorRuntimeStatus(editorWindow)}if(editorWindow.editorRuntimeState==="error"){return"\u7F16\u8F91\u5668\u964D\u7EA7"}if(editorDirty(editorWindow)){return"\u672A\u4FDD\u5B58"}return editorWindow.message||"\u5DF2\u4FDD\u5B58"}function editorOpenStatus(editorWindow){const progress=editorWindow.openProgress||{};const stage=progress.stage||"preparing";const loaded=Number(progress.loadedBytes)||0;const total=Number(progress.totalBytes)||Number(editorWindow.size)||0;if(stage==="downloading"||loaded>0){const percentText=total>0?`${Math.min(100,Math.max(0,Math.round(loaded/total*100)))}%`:formatSize(loaded);return`\u4E0B\u8F7D\u4E2D ${percentText} \xB7 ${formatEditorBytes(loaded,total)} \xB7 ${formatUploadSpeed(editorOpenSpeed(editorWindow))}`}if(stage==="error"){return progress.message||"\u6253\u5F00\u5931\u8D25"}if(stage==="stat"){return"\u6253\u5F00\u4E2D \xB7 \u8BFB\u53D6\u8FDC\u7A0B\u6587\u4EF6\u4FE1\u606F"}return progress.message||"\u6253\u5F00\u4E2D \xB7 \u5EFA\u7ACB\u8FDC\u7A0B\u8BFB\u53D6"}function editorRuntimeStatus(editorWindow){const message=editorWindow.editorRuntimeMessage||"\u52A0\u8F7D Monaco...";const progress=Number(editorWindow.editorRuntimeProgress)||0;if(progress>0){return`${message} \xB7 ${Math.round(progress*100)}%`}if(editorWindow.editorRuntimeStep&&editorWindow.editorRuntimeTotalSteps){return`${message} \xB7 ${editorWindow.editorRuntimeStep}/${editorWindow.editorRuntimeTotalSteps}`}return message}function editorOpenSpeed(editorWindow){const progress=editorWindow.openProgress||{};const loaded=Number(progress.loadedBytes)||0;const elapsedSeconds=Math.max((Date.now()-(Number(progress.startedAt)||Date.now()))/1e3,.1);return loaded/elapsedSeconds}function formatEditorBytes(loaded,total){if(total>0){return`${formatSize(loaded)} / ${formatSize(total)}`}if(loaded>0){return`${formatSize(loaded)} \u5DF2\u4E0B\u8F7D`}return"-"}function setEditorRuntimeState(editorWindow,state){if(!editorWindow){return}const previousState=editorWindow.editorRuntimeState;const previousMessage=editorWindow.editorRuntimeMessage;editorWindow.editorRuntimeState=state?.status||"";editorWindow.editorRuntimeMessage=state?.message||"";editorWindow.editorRuntimeProgress=Math.max(0,Math.min(1,Number(state?.progress)||0));editorWindow.editorRuntimeStep=Number(state?.step)||0;editorWindow.editorRuntimeTotalSteps=Number(state?.totalSteps)||0;if(state?.status==="error"){editorWindow.error=state.message||"Monaco \u52A0\u8F7D\u5931\u8D25\uFF0C\u5DF2\u5207\u6362\u5230\u57FA\u7840\u7F16\u8F91\u6A21\u5F0F";return}if(previousState==="error"&&editorWindow.error===previousMessage){editorWindow.error=""}}function updateEditorOpenProgress(editorWindow,progress={}){if(!editorWindow){return}const existing=editorWindow.openProgress||{};const totalBytes=Number(progress.totalBytes)||Number(existing.totalBytes)||Number(editorWindow.size)||0;editorWindow.openProgress={stage:progress.stage||existing.stage||"downloading",loadedBytes:Math.max(Number(existing.loadedBytes)||0,Number(progress.loadedBytes)||0),totalBytes,startedAt:existing.startedAt||Date.now(),message:progress.message||existing.message||""}}function editorMeta(editorWindow){if(!editorWindow){return""}const parts=[formatSize(new Blob([editorWindow.content]).size)];if(editorWindow.modTime){parts.push(formatTime(editorWindow.modTime))}return parts.join(" \xB7 ")}function editorWindowStyle(editorWindow){if(editorWindow.windowState==="minimized"){const minimizedIndex=editors.value.filter(item=>item.windowState==="minimized").findIndex(item=>item.id===editorWindow.id);return{left:`${16+Math.max(0,minimizedIndex)*252}px`,bottom:"14px",width:"240px",height:"38px",zIndex:editorWindow.zIndex}}if(editorWindow.windowState==="maximized"){return{left:"12px",top:"50px",width:"calc(100vw - 24px)",height:"calc(100vh - 62px)",zIndex:editorWindow.zIndex}}return{left:`${editorWindow.x}px`,top:`${editorWindow.y}px`,width:`${editorWindow.width}px`,height:`${editorWindow.height}px`,zIndex:editorWindow.zIndex}}function minimizeEditor(editorWindow){if(!editorWindow){return}editorWindow.windowState="minimized";activateEditor(editorWindow.id)}function toggleMaximizeEditor(editorWindow){if(!editorWindow){return}editorWindow.windowState=editorWindow.windowState==="normal"?"maximized":"normal";activateEditor(editorWindow.id)}function startEditorDrag(event,editorWindow){if(!editorWindow||editorWindow.windowState!=="normal"||event.button!==0){return}activateEditor(editorWindow.id);editorDragState={id:editorWindow.id,startX:event.clientX,startY:event.clientY,originX:editorWindow.x,originY:editorWindow.y};document.body.style.cursor="move";document.body.style.userSelect="none";window.addEventListener("mousemove",onEditorDrag);window.addEventListener("mouseup",stopEditorDrag)}function onEditorDrag(event){if(!editorDragState){return}const editorWindow=editors.value.find(item=>item.id===editorDragState.id);if(!editorWindow){stopEditorDrag();return}const maxX=Math.max(16,(window.innerWidth||1200)-120);const maxY=Math.max(MIN_EDITOR_TOP,(window.innerHeight||780)-60);editorWindow.x=Math.min(maxX,Math.max(16,editorDragState.originX+event.clientX-editorDragState.startX));editorWindow.y=Math.min(maxY,Math.max(MIN_EDITOR_TOP,editorDragState.originY+event.clientY-editorDragState.startY))}function stopEditorDrag(){if(!editorDragState){return}editorDragState=null;document.body.style.cursor="";document.body.style.userSelect="";window.removeEventListener("mousemove",onEditorDrag);window.removeEventListener("mouseup",stopEditorDrag)}async function saveEditor(editorWindow){if(!props.connectionId||!editorWindow||!editorWindow.path||editorWindow.loading||editorWindow.saving){return false}editorWindow.saving=true;editorWindow.error="";editorWindow.message="";const content=editorWindow.content;try{const result=await saveRemoteTextFile(props.connectionId,editorWindow.path,content);const file=result.file||{};editorWindow.path=String(file.path||editorWindow.path);editorWindow.name=String(file.name||editorWindow.name||displayPath(editorWindow.path));editorWindow.originalContent=content;editorWindow.size=Number(file.size)||new Blob([content]).size;editorWindow.modTime=String(file.modTime||"");editorWindow.message="\u5DF2\u4FDD\u5B58";invalidateDirectoryCache(parentPath(editorWindow.path)||currentPath.value||initialPathForMode(props.workMode));await refresh(currentPath.value||initialPathForMode(props.workMode),{useCache:false});return true}catch(error){editorWindow.error=error instanceof Error?error.message:"\u4FDD\u5B58\u5931\u8D25";return false}finally{editorWindow.saving=false}}function requestEditorClose(editorWindow,options={}){const afterClose=typeof options.afterClose==="function"?options.afterClose:null;if(!editorWindow||!editorDirty(editorWindow)){closeEditorImmediately(editorWindow);runAfterEditorClose(afterClose);return}editorWindow.closePrompt.visible=true;editorWindow.closePrompt.afterClose=afterClose;if(editorWindow.windowState==="minimized"){editorWindow.windowState="normal"}activateEditor(editorWindow.id)}function closeEditorImmediately(editorWindow){if(!editorWindow){return}const index=editors.value.findIndex(item=>item.id===editorWindow.id);if(index===-1){return}editors.value.splice(index,1);if(activeEditorId.value===editorWindow.id){const next=[...editors.value].sort((left,right)=>right.zIndex-left.zIndex)[0];activeEditorId.value=next?.id||""}}async function runAfterEditorClose(afterClose){if(typeof afterClose==="function"){await afterClose()}}async function saveEditorFromPrompt(editorWindow){const saved=await saveEditor(editorWindow);if(saved){editorWindow.closePrompt.visible=false;editorWindow.closePrompt.afterClose=null}}async function saveAndCloseEditorFromPrompt(editorWindow){const afterClose=editorWindow.closePrompt.afterClose;const saved=await saveEditor(editorWindow);if(!saved){return}closeEditorImmediately(editorWindow);await runAfterEditorClose(afterClose)}async function discardEditorFromPrompt(editorWindow){const afterClose=editorWindow.closePrompt.afterClose;closeEditorImmediately(editorWindow);await runAfterEditorClose(afterClose)}function cancelEditorClosePrompt(editorWindow){if(!editorWindow){return}editorWindow.closePrompt.visible=false;editorWindow.closePrompt.afterClose=null}function selectEntry(entry,index,event){if(event.shiftKey&&lastSelectedIndex.value>=0){const start=Math.min(lastSelectedIndex.value,index);const end=Math.max(lastSelectedIndex.value,index);const next=new Set(selectedPaths.value);for(let i=start;i<=end;i+=1){next.add(orderedEntries.value[i].path)}selectedPaths.value=next;return}if(event.ctrlKey||event.metaKey){const next=new Set(selectedPaths.value);if(next.has(entry.path)){next.delete(entry.path)}else{next.add(entry.path)}selectedPaths.value=next;lastSelectedIndex.value=index;return}selectedPaths.value=new Set([entry.path]);lastSelectedIndex.value=index}function isSelected(entry){return selectedPaths.value.has(entry.path)}function clearSelection(){selectedPaths.value=new Set;lastSelectedIndex.value=-1}function keepExistingSelection(){const available=new Set(entries.value.map(entry=>entry.path));selectedPaths.value=new Set(Array.from(selectedPaths.value).filter(itemPath=>available.has(itemPath)))}function onPaneClick(event){if(!event.target.closest(".file-row")){clearSelection()}hideContextMenu()}function onShellClick(){hideContextMenu();collapseUploadPanel();pathHistoryOpen.value=false}function openEntryContextMenu(event,entry,index){if(!selectedPaths.value.has(entry.path)){selectedPaths.value=new Set([entry.path]);lastSelectedIndex.value=index}openContextMenu(event,{entry,targetPath:entry.isDir?entry.path:defaultDirectoryPath(),targetKind:entry.isDir?"directory":"file"})}function openPathContextMenu(event,targetPath){openContextMenu(event,{targetPath,targetKind:"directory"})}function openBlankContextMenu(event){openContextMenu(event,{targetPath:defaultDirectoryPath(),targetKind:"blank"})}function openContextMenu(event,target={}){const position=viewportContextMenuPosition(event,{width:220,height:470});contextMenu.visible=true;contextMenu.entry=target.entry||null;contextMenu.targetPath=target.targetPath||defaultDirectoryPath();contextMenu.targetKind=target.targetKind||"blank";contextMenu.x=position.x;contextMenu.y=position.y}function hideContextMenu(){contextMenu.visible=false}function onGlobalPointerDown(event){const target=event.target;if(pathHistoryOpen.value&&target instanceof Element){if(!target.closest(".path-history-popover")&&!target.closest(".path-history-button")){pathHistoryOpen.value=false}}if(contextMenu.visible){if(target instanceof Element&&target.closest(".file-context-menu")){return}hideContextMenu()}}async function refreshFromMenu(){const target=contextMenu.targetPath||currentPath.value||initialPathForMode(props.workMode);hideContextMenu();await refreshOpenDirectories(target)}function copySelection(){writeClipboard("copy")}function cutSelection(){writeClipboard("move")}function copySelectionFromMenu(){hideContextMenu();copySelection()}function cutSelectionFromMenu(){hideContextMenu();cutSelection()}async function deleteFromMenu(){hideContextMenu();if(hasSelection.value){await deleteSelection();return}const item=contextDeleteItem.value;if(item){await deleteItems([item])}}async function deleteSelection(){if(!hasSelection.value||!props.connectionId){return}await deleteItems(selectedEntries.value)}async function deleteItems(items){if(!items.length||!props.connectionId){return}const selected=items.map(item=>({path:item.path,isDir:Boolean(item.isDir)}));if(!window.confirm(deleteConfirmMessage(selected))){return}loading.value=true;errorMessage.value="";try{await deleteRemoteItems(props.connectionId,selected.map(entry=>({path:entry.path,isDir:entry.isDir})));const nextPath=pathAfterDeletingItems(selected);syncDirectoryCacheAfterDelete(selected);clearSelection();clearClipboardIfDeleted(selected);await refresh(nextPath,{useCache:false})}catch(error){errorMessage.value=error instanceof Error?error.message:"\u5220\u9664\u5931\u8D25"}finally{loading.value=false}}function deleteConfirmMessage(selected){const preview=selected.slice(0,6).map(entry=>`- ${entry.path}`).join("\n");const more=selected.length>6?`
... \u8FD8\u6709 ${selected.length-6} \u9879`:"";return`\u786E\u8BA4\u5F3A\u5236\u5220\u9664\u9009\u4E2D\u7684 ${selected.length} \u9879\uFF1F

${preview}${more}

\u6B64\u64CD\u4F5C\u4E0D\u53EF\u64A4\u9500\u3002`}function writeClipboard(action){if(!hasSelection.value){return}const normalizedAction=normalizeClipboardAction(action);const payload={sourceConnectionId:props.connectionId,action:normalizedAction,items:selectedEntries.value.map(entry=>({path:entry.path,isDir:entry.isDir})),createdAt:Date.now()};localStorage.setItem(CLIPBOARD_KEY,JSON.stringify(payload));clipboard.value=payload}async function pasteClipboard(targetPath=contextTargetDir.value){if(!canPaste.value){return}const pendingClipboard=normalizeClipboardPayload(clipboard.value);if(!pendingClipboard){clipboard.value=null;localStorage.removeItem(CLIPBOARD_KEY);return}loading.value=true;errorMessage.value="";try{await transferRemoteItems({sourceConnectionId:pendingClipboard.sourceConnectionId,targetConnectionId:props.connectionId,targetPath:targetPath||defaultDirectoryPath(),action:pendingClipboard.action,items:pendingClipboard.items});invalidateDirectoryCache(targetPath||defaultDirectoryPath());if(pendingClipboard.action==="move"&&pendingClipboard.sourceConnectionId===props.connectionId){for(const item of pendingClipboard.items){invalidateDirectoryCache(parentPath(item.path))}}if(pendingClipboard.action==="move"){localStorage.removeItem(CLIPBOARD_KEY);clipboard.value=null}await refresh(currentPath.value||initialPathForMode(props.workMode),{useCache:false})}catch(error){errorMessage.value=error instanceof Error?error.message:"\u7C98\u8D34\u5931\u8D25"}finally{loading.value=false}}async function pasteClipboardFromMenu(){const target=contextTargetDir.value;hideContextMenu();await pasteClipboard(target)}function copyPathFromMenu(){const value=contextPath.value;hideContextMenu();if(value){navigator.clipboard?.writeText(value).catch(()=>{})}}function onRemoteDragStart(event,entry,index){if(!selectedPaths.value.has(entry.path)){selectedPaths.value=new Set([entry.path]);lastSelectedIndex.value=index}const selected=selectedEntries.value.length?selectedEntries.value:[entry];const paths=selected.map(item=>item.path);const fileName=selected.length===1&&!selected[0].isDir?selected[0].name:downloadArchiveName(selected);const url=selected.length===1&&!selected[0].isDir?backendDownloadUrl(`/api/sftp/download?connectionId=${encodeURIComponent(props.connectionId)}&path=${encodeURIComponent(selected[0].path)}`):archiveRemoteItemsUrl(props.connectionId,paths);const absoluteUrl=new URL(url,window.location.origin).toString();event.dataTransfer.effectAllowed="copy";event.dataTransfer.setData("DownloadURL",`application/octet-stream:${fileName}:${absoluteUrl}`);event.dataTransfer.setData("text/uri-list",absoluteUrl);event.dataTransfer.setData("text/plain",absoluteUrl)}function deriveDirectoriesFromFiles(files){const directories=new Set;for(const file of files){const relativePath=file.webkitRelativePath||"";const parts=relativePath.split("/").filter(Boolean);parts.pop();for(let i=1;i<=parts.length;i+=1){directories.add(parts.slice(0,i).join("/"))}}return Array.from(directories)}async function collectDroppedItems(dataTransfer){const entriesFromItems=Array.from(dataTransfer.items||[]).map(item=>typeof item.webkitGetAsEntry==="function"?item.webkitGetAsEntry():null).filter(Boolean);if(entriesFromItems.length===0){return{files:Array.from(dataTransfer.files||[]).map(file=>({file,relativePath:file.name})),directories:[]}}const files=[];const directories=new Set;for(const entry of entriesFromItems){await walkDroppedEntry(entry,"",files,directories)}return{files,directories:Array.from(directories)}}async function walkDroppedEntry(entry,parentPath2,files,directories){const relativePath=parentPath2?`${parentPath2}/${entry.name}`:entry.name;if(entry.isFile){const file=await fileFromEntry(entry);files.push({file,relativePath});return}if(!entry.isDirectory){return}directories.add(relativePath);const children=await readAllDirectoryEntries(entry.createReader());for(const child of children){await walkDroppedEntry(child,relativePath,files,directories)}}function fileFromEntry(entry){return new Promise((resolve,reject)=>{entry.file(resolve,reject)})}function readAllDirectoryEntries(reader){return new Promise((resolve,reject)=>{const entries2=[];const readBatch=()=>{reader.readEntries(batch=>{if(batch.length===0){resolve(entries2);return}entries2.push(...batch);readBatch()},error=>reject(error))};readBatch()})}function startUploadProgress(items,directories,targetPath){clearUploadCloseTimer();const files=items.map((item,index)=>({id:`${Date.now()}-${index}-${item.relativePath||item.file?.name||"file"}`,relativePath:item.relativePath||item.file?.webkitRelativePath||item.file?.name||`file-${index+1}`,size:Number(item.file?.size)||0,loaded:0}));uploadProgress.visible=true;uploadProgress.expanded=true;uploadProgress.status="uploading";uploadProgress.targetPath=targetPath;uploadProgress.files=files;uploadProgress.directoryCount=directories.length;uploadProgress.totalBytes=files.reduce((total,item)=>total+item.size,0);uploadProgress.loadedBytes=0;uploadProgress.speed=0;uploadProgress.startedAt=Date.now();uploadProgress.message=""}function onUploadProgress(progress){if(!uploadProgress.visible||uploadProgress.status!=="uploading"){return}const loaded=Number(progress?.loadedBytes??progress?.loaded)||0;const total=Number(progress?.totalBytes??progress?.total)||0;if(total>uploadProgress.totalBytes){uploadProgress.totalBytes=total}const cappedLoaded=uploadProgress.totalBytes>0?Math.min(uploadProgress.totalBytes,loaded):loaded;uploadProgress.loadedBytes=Math.max(uploadProgress.loadedBytes,cappedLoaded);distributeUploadLoaded(uploadProgress.loadedBytes);const elapsedSeconds=Math.max((Date.now()-uploadProgress.startedAt)/1e3,.1);uploadProgress.speed=uploadProgress.loadedBytes/elapsedSeconds}function distributeUploadLoaded(loadedBytes){let remaining=loadedBytes;for(const item of uploadProgress.files){item.loaded=Math.min(item.size,Math.max(0,remaining));remaining-=item.size}}function markUploadComplete(){uploadProgress.status="done";uploadProgress.loadedBytes=uploadProgress.totalBytes;distributeUploadLoaded(uploadProgress.totalBytes);uploadProgress.speed=0;uploadProgress.message="\u4E0A\u4F20\u5DF2\u5B8C\u6210"}function markUploadError(message){uploadProgress.status="error";uploadProgress.expanded=true;uploadProgress.message=message||"\u4E0A\u4F20\u5931\u8D25"}function scheduleUploadPanelClose(){clearUploadCloseTimer();uploadCloseTimer=window.setTimeout(()=>{collapseUploadPanel();uploadCloseTimer=null},UPLOAD_CLOSE_DELAY_MS)}function clearUploadCloseTimer(){if(uploadCloseTimer){window.clearTimeout(uploadCloseTimer);uploadCloseTimer=null}}function collapseUploadPanel(){if(uploadProgress.visible){uploadProgress.expanded=false}}function expandUploadPanel(){if(uploadProgress.visible){clearUploadCloseTimer();uploadProgress.expanded=true}}function uploadFilePercent(item){if(uploadProgress.status==="done"){return 100}if(!item.size){return uploadProgress.status==="uploading"?0:100}return Math.min(100,Math.max(0,Math.round(item.loaded/item.size*100)))}function formatUploadSpeed(bytesPerSecond){if(!bytesPerSecond||bytesPerSecond<1){return"-"}return`${formatSize(bytesPerSecond)}/s`}function readClipboard(){try{const raw=localStorage.getItem(CLIPBOARD_KEY);if(!raw){return null}return normalizeClipboardPayload(JSON.parse(raw))}catch{return null}}function normalizeClipboardPayload(value){if(!value?.sourceConnectionId||!Array.isArray(value.items)||value.items.length===0){return null}const items=value.items.filter(item=>item?.path).map(item=>({path:item.path,isDir:Boolean(item.isDir)}));if(items.length===0){return null}return{sourceConnectionId:value.sourceConnectionId,action:normalizeClipboardAction(value.action),items,createdAt:Number(value.createdAt)||Date.now()}}function normalizeClipboardAction(action){return CLIPBOARD_ACTIONS.has(action)?action:"copy"}function clearClipboardIfDeleted(deletedItems){const pendingClipboard=normalizeClipboardPayload(clipboard.value);if(!pendingClipboard){return}const removed=deletedItems.some(deletedItem=>pendingClipboard.items.some(item=>item.path===deletedItem.path||deletedItem.isDir&&isSameOrChildPath(item.path,deletedItem.path)));if(!removed){return}localStorage.removeItem(CLIPBOARD_KEY);clipboard.value=null}function onStorage(event){if(event.key===CLIPBOARD_KEY){clipboard.value=readClipboard()}}function onKeydown(event){const key=event.key.toLowerCase();const currentEditor=activeEditor.value;if((event.ctrlKey||event.metaKey)&&key==="s"&&currentEditor){event.preventDefault();saveEditor(currentEditor);return}if(event.key==="Escape"){if(currentEditor?.closePrompt.visible){cancelEditorClosePrompt(currentEditor);return}hideContextMenu()}if(event.key==="F5"&&props.connectionId){event.preventDefault();refreshOpenDirectories(currentPath.value||initialPathForMode(props.workMode))}}function startColumnResize(event,key){resizeState={key,startX:event.clientX,startWidth:columnWidths[key]};document.body.style.cursor="col-resize";document.body.style.userSelect="none";window.addEventListener("mousemove",onColumnResize);window.addEventListener("mouseup",stopColumnResize)}function onColumnResize(event){if(!resizeState){return}const nextWidth=Math.max(MIN_COLUMN_WIDTH,resizeState.startWidth+event.clientX-resizeState.startX);columnWidths[resizeState.key]=nextWidth;applyColumnWidths()}function stopColumnResize(){if(!resizeState){return}resizeState=null;document.body.style.cursor="";document.body.style.userSelect="";window.removeEventListener("mousemove",onColumnResize);window.removeEventListener("mouseup",stopColumnResize)}function loadColumnWidths(){const values={};for(const column of columns){const raw=document.documentElement.style.getPropertyValue(`--file-col-${column.key}`);const parsed=Number.parseInt(raw,10);values[column.key]=Number.isFinite(parsed)?parsed:column.width}return values}function applyColumnWidths(){for(const column of columns){document.documentElement.style.setProperty(`--file-col-${column.key}`,`${columnWidths[column.key]}px`)}}function normalizePath(value){const raw=String(value||"").trim();if(!raw){return""}if(raw===ROOT_PATH||raw===HOME_PATH){return raw}if(raw.startsWith("~/")){const parts=raw.slice(2).split("/").filter(Boolean);return parts.length?`${HOME_PATH}/${parts.join("/")}`:HOME_PATH}if(raw.startsWith(ROOT_PATH)){const parts=raw.split("/").filter(Boolean);return parts.length?`${ROOT_PATH}${parts.join("/")}`:ROOT_PATH}return raw}function parentPath(itemPath){const normalized=normalizePath(itemPath);if(!normalized||normalized===ROOT_PATH||normalized===HOME_PATH){return""}if(normalized.startsWith("~/")){const parts=normalized.slice(2).split("/").filter(Boolean);if(parts.length<=1){return HOME_PATH}return`${HOME_PATH}/${parts.slice(0,-1).join("/")}`}if(normalized.startsWith(ROOT_PATH)){const parts=normalized.split("/").filter(Boolean);if(parts.length<=1){return ROOT_PATH}return`${ROOT_PATH}${parts.slice(0,-1).join("/")}`}return""}function pathDepth(itemPath){if(itemPath===ROOT_PATH||itemPath===HOME_PATH){return 0}if(itemPath.startsWith("~/")){return itemPath.slice(2).split("/").filter(Boolean).length}return itemPath.split("/").filter(Boolean).length}function isSameOrChildPath(candidate,parent){const cleanCandidate=normalizePath(candidate);const cleanParent=normalizePath(parent);if(!cleanCandidate||!cleanParent){return false}if(cleanCandidate===cleanParent){return true}if(cleanParent===ROOT_PATH){return cleanCandidate.startsWith(ROOT_PATH)}return cleanCandidate.startsWith(`${cleanParent}/`)}function isPathVisible(itemPath){if(itemPath===ROOT_PATH||itemPath===HOME_PATH){return true}let cursor=parentPath(itemPath);while(cursor){if(pathMeta.value.get(cursor)?.collapsed){return false}cursor=parentPath(cursor)}return true}function comparePaths(a,b){if(a===b){return 0}if(a===ROOT_PATH){return-1}if(b===ROOT_PATH){return 1}const aHome=a===HOME_PATH||a.startsWith("~/");const bHome=b===HOME_PATH||b.startsWith("~/");if(aHome!==bHome){return aHome?1:-1}return a.localeCompare(b,void 0,{sensitivity:"base"})}function buildBreadcrumbs(itemPath){const normalized=normalizePath(itemPath);if(!normalized){return[]}if(normalized===HOME_PATH){return[{label:HOME_PATH,path:HOME_PATH}]}if(normalized.startsWith("~/")){const parts=normalized.slice(2).split("/").filter(Boolean);const crumbs=[{label:HOME_PATH,path:HOME_PATH}];for(let index=0;index<parts.length;index+=1){crumbs.push({label:parts[index],path:`${HOME_PATH}/${parts.slice(0,index+1).join("/")}`})}return crumbs}if(normalized.startsWith(ROOT_PATH)){const parts=normalized.split("/").filter(Boolean);const crumbs=[{label:ROOT_PATH,path:ROOT_PATH}];for(let index=0;index<parts.length;index+=1){crumbs.push({label:parts[index],path:`${ROOT_PATH}${parts.slice(0,index+1).join("/")}`})}return crumbs}return[{label:normalized,path:normalized}]}function displayPath(itemPath){const normalized=normalizePath(itemPath);if(normalized===ROOT_PATH||normalized===HOME_PATH){return normalized}if(normalized.startsWith("~/")){const parts2=normalized.slice(2).split("/").filter(Boolean);return parts2.at(-1)||HOME_PATH}const parts=normalized.split("/").filter(Boolean);return parts.at(-1)||normalized}function downloadArchiveName(selected){if(selected.length===1){return`${selected[0].name}.zip`}return"zshell-selected.zip"}function delay(ms){return new Promise(resolve=>{window.setTimeout(resolve,ms)})}function formatSize(size){if(typeof size!=="number"){return"-"}if(size<1024){return`${Math.max(0,Math.round(size))} B`}if(size<1024*1024){return`${(size/1024).toFixed(1)} KB`}if(size<1024*1024*1024){return`${(size/(1024*1024)).toFixed(1)} MB`}return`${(size/(1024*1024*1024)).toFixed(1)} GB`}function formatTime(iso){if(!iso){return"-"}const date=new Date(iso);if(Number.isNaN(date.getTime())){return iso}return date.toLocaleString()}return{ROOT_PATH,HOME_PATH,WORK_MODE_START_PATHS,CLIPBOARD_KEY,DIRECTORY_CACHE_LIMIT,MAX_PRELOAD_CONCURRENCY,TREE_INDEX_BATCH_SIZE,TREE_INDEX_DELAY_MS,PRELOAD_BATCH_SIZE,PRELOAD_BATCH_DELAY_MS,REFRESH_BATCH_SIZE,REFRESH_BATCH_DELAY_MS,MIN_COLUMN_WIDTH,UPLOAD_CLOSE_DELAY_MS,SORT_PULSE_MS,DEFAULT_FILE_OPEN_ACTION,DEFAULT_EDITOR_WIDTH,DEFAULT_EDITOR_HEIGHT,MIN_EDITOR_TOP,CLIPBOARD_ACTIONS,directoryCache,columns,fileOpenActions,entries,currentPath,pathDraft,loading,refreshingCached,uploading,dragOver,errorMessage,navCollapsed,selectedPaths,lastSelectedIndex,clipboard,fileInput,folderInput,pathHistoryButton,pathHistoryListRef,pendingUploadPath,pathHistoryStats,pathHistoryOpen,columnWidths,sortState,sortPulseKey,uploadProgress,editors,activeEditorId,pathMeta,resizeState,editorDragState,uploadCloseTimer,editorZIndex,refreshSerial,preloadSerial,preloadController,treeIndexQueue,treeIndexTimer,sortPulseTimer,contextMenu,pathHistoryPanel,orderedEntries,selectedEntries,hasSelection,canPaste,contextEntry,contextCanOpenDirectory,contextPath,contextTargetDir,contextDeleteItem,canDeleteFromMenu,gridTemplateColumns,activeEditor,preloadConcurrency,statusText,breadcrumbs,pathHistoryStyle,uploadPercent,uploadTitle,uploadDetail,pathHistory,compareEntriesForSort,compareEntryValue,compareText,compareNumber,changeSort,sortArrow,pulseSortColumn,clearSortPulseTimer,treeNodes,refresh,loadDirectoryFromRemote,refreshOpenDirectories,openedDirectoryPaths,directoryListingMatchesFocus,applyDirectoryListing,resetPathState,initialPathForMode,defaultDirectoryPath,normalizeWorkMode,initialPathMeta,getCachedDirectory,setCachedDirectory,directoryCacheKey,cloneEntries,rememberDirectoryTree,scheduleDirectoryPreload,cancelDirectoryPreload,directoryPreloadTargets,enqueueTreeIndex,scheduleTreeIndexWork,processTreeIndexBatch,clearTreeIndexQueue,rememberPath,rememberParentChain,rememberParentChainInMap,mergePathMeta,rememberPathHistory,togglePathHistory,positionPathHistoryPanel,trimPathHistoryStats,scrollPathHistoryToBottom,openHistoryPath,togglePathNode,openDir,openEntry,onPathInput,commitPathDraft,completePathDraft,ensureCompletionCaches,pathCompletionMatches,pathCompletionCandidates,completionMatchesForPrefix,currentRelativeCompletionPrefix,completionParentPath,completionTarget,cachedDirectoriesForConnection,syncDirectoryCacheAfterDelete,invalidateDirectoryCache,entryMatchesDeletedItem,removeDeletedPathMeta,pathAfterDeletingItems,nearestExistingParentAfterDelete,isDeletableRemotePath,chooseFiles,chooseFolder,chooseFilesFromMenu,chooseFolderFromMenu,onFilePickerUpload,onFolderPickerUpload,uploadPickedFiles,uploadItems,onDragOver,onDragLeave,onDropUpload,download,downloadSelection,downloadSelectionFromMenu,downloadContextEntry,openContextDirectory,runContextFileOpenAction,runFileOpenAction,openTextEditor,createEditorWindow,defaultEditorBounds,nextEditorZIndex,activateEditor,editorDirty,editorTitle,editorStatus,editorOpenStatus,editorRuntimeStatus,editorOpenSpeed,formatEditorBytes,setEditorRuntimeState,updateEditorOpenProgress,editorMeta,editorWindowStyle,minimizeEditor,toggleMaximizeEditor,startEditorDrag,onEditorDrag,stopEditorDrag,saveEditor,requestEditorClose,closeEditorImmediately,runAfterEditorClose,saveEditorFromPrompt,saveAndCloseEditorFromPrompt,discardEditorFromPrompt,cancelEditorClosePrompt,selectEntry,isSelected,clearSelection,keepExistingSelection,onPaneClick,onShellClick,openEntryContextMenu,openPathContextMenu,openBlankContextMenu,openContextMenu,hideContextMenu,onGlobalPointerDown,refreshFromMenu,copySelection,cutSelection,copySelectionFromMenu,cutSelectionFromMenu,deleteFromMenu,deleteSelection,deleteItems,deleteConfirmMessage,writeClipboard,pasteClipboard,pasteClipboardFromMenu,copyPathFromMenu,onRemoteDragStart,deriveDirectoriesFromFiles,collectDroppedItems,walkDroppedEntry,fileFromEntry,readAllDirectoryEntries,startUploadProgress,onUploadProgress,distributeUploadLoaded,markUploadComplete,markUploadError,scheduleUploadPanelClose,clearUploadCloseTimer,collapseUploadPanel,expandUploadPanel,uploadFilePercent,formatUploadSpeed,readClipboard,normalizeClipboardPayload,normalizeClipboardAction,clearClipboardIfDeleted,onStorage,onKeydown,startColumnResize,onColumnResize,stopColumnResize,loadColumnWidths,applyColumnWidths,normalizePath,parentPath,pathDepth,isSameOrChildPath,isPathVisible,comparePaths,buildBreadcrumbs,displayPath,downloadArchiveName,delay,formatSize,formatTime}}export{useFileManager};
